# =========================================================================
# Variables de entorno — Blackprint Map Viewer
# =========================================================================
# Copia este archivo a .env.local y rellena los valores.
# En Vercel, configúralas en Settings → Environment Variables.
# =========================================================================

# Nombre del bucket de GCS (sin gs://)
GCS_BUCKET=tu-bucket-aqui

# Prefijo dentro del bucket donde están los archivos
# (porque subiste la carpeta TO_GOOGLE_CLOUD directamente)
GCS_PREFIX=TO_GOOGLE_CLOUD

# Credenciales del Service Account (JSON completo pegado como string)
# Opción 1: JSON crudo en env var (recomendado para Vercel)
BQ_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Opción 2: ruta a archivo JSON (para desarrollo local)
# GOOGLE_APPLICATION_CREDENTIALS=/ruta/al/archivo.json

